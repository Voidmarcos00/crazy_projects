import re

def strong_password(password):
    
    if len(password) < 8:
        return False
    
    if not re.search("[A-Z]", password):
        return False
    
    if not re.search("[a-z]", password):
        return False
    
    if not re.search("[0-9]", password):
        return False
    
    if not re.search("[!@#$%^&*()-_=+{};:,<.>]", password):
        return False
    
    return True

# Test the function
# while True:
print("The password must be at least 8 characters long.")
print("The password must contain at least one uppercase letter.")
print("The password must contain at least one lowercase letter.")
print("The password must contain at least one digit.")
print("The password must contain at least one special character.")
password1 = input()
password2 = input()
print("Password 1 is strong:", strong_password(password1))
print("Password 2 is strong:", strong_password(password2))
